name: validate-fog-bfloat16-rounding-fix

on:
  push:
    branches:
      - agent/fix-fog-bfloat16-rounding
    paths:
      - ".github/workflows/fog-bfloat16-rounding-fix.yml"

permissions:
  contents: write

jobs:
  validate-and-materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-fog-bfloat16-rounding
      - name: Apply finite-precision projection fix
        shell: bash
        run: |
          set -euo pipefail
          git apply --check .bootstrap/fog_rounding_fix.patch
          git apply .bootstrap/fog_rounding_fix.patch
          rm -f .bootstrap/fog_rounding_fix.patch
          rmdir .bootstrap 2>/dev/null || true
          python -m compileall -q experiments/frozen_operator_graph
          bash -n experiments/frozen_operator_graph/run_qa.sh
      - name: Run complete frozen operator graph tests
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install torch pytest
          pytest -q experiments/frozen_operator_graph/tests
      - name: Commit tested source fix
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix bfloat16 operator projection validation"
          git push origin HEAD:agent/fix-fog-bfloat16-rounding
