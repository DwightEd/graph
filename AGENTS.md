# 本项目当前工作约定

每次修改代码、重构或运行实验前，先阅读本文件及适用的项目规范。

## 默认交付：直接合并并推送 main

用户已授权本项目代码任务默认直接提交到 `DwightEd/graph` 的 `main`。除非用户另有指示，完成当前授权范围的修改和测试后，合并到最新 `main` 并直接推送；不另建分支或 PR，不反复询问是否推送，不以下载补丁或让用户手动应用代替正常交付。
推送前读取远端最新提交，保留用户及其他会话的并发修改，只提交本次任务文件；不强制推送，不重置或覆盖已有历史。
使用已关联的 GitHub 工具完成写入，推送后回读远端 `main` 的提交 SHA，核实实际生效，再报告提交号和运行命令。
若权限、分支保护或安全限制阻止推送，如实说明实际失败及尚未提交的内容；不得绕过限制或声称已推送。

## 代码可读性与维护性

遵循项目格式与命名约定，不以牺牲可读性换取更少行数或输出 token；复杂操作清晰分步，合理消除重复。避免用未说明的裸下标或平行数组隐含业务关系；性能布局、协议或算法确需时可保留，但须明确并验证映射与相关不变量，不强制增加对象层。领域阈值、缩放和位掩码按需具名，优先复用现有定义，单位或依据不自明时补充说明；普通计数、协议字面量、哈希及测试边界值不机械抽象。函数职责聚焦，不为抽象增加框架、耦合或间接层。测试诊断应定位失败条件，仅输出必要的非敏感信息。行为保持型整理不得改变公开结果、错误契约、事务及安全边界，并按风险回归；格式检查不替代语义与可维护性审查。仅处理当前授权范围，历史原始证据不为美化改写。

## 研究与运行约定

修改代码前读 `iclr/CODING_GUIDELINES.md`、`iclr/MECHANISM_FIRST.md`。
原始逐 token R/A/H 基线是 `main.py support`，协议见 `iclr/TOKEN_DETECTION.md`。
当前来源传递候选是 `main.py transport`，先读 `iclr/VALUE_PATH_TRANSPORT.md`。
用户明确要求借鉴其他成熟研究，不只组合前几轮实验。采用 DecompX 的跨层来源分解思想、
ALTI-Logit 的输出读出、乘法相关性分配和 Information Flow 的路径视角。
原生前向不改；归因固定 attention pattern/RMS scale，SiLU 割线 + 乘法等分。
这是指定的值路径分解，不是 native Jacobian，不包含 Q/K 路由变化的因果效应。
当前布局要求 bias-free value/MLP/output 和 SiLU，其他布局必须明确适配。
输入根作用按候选保存，保留层头消息诊断；层间作用不能当作独立贡献相加。
FFN 已进入根归因，不把反向写入自动判为反证据，不把它重复加入风险。
`value_transport/` 是新缓存/结果；不能从旧 detached-KV 缓存恢复根来源。
一次完整前向，独立目标反向，SDPA + FFN checkpoint；默认梯度批量 1。
不使用自然标签拟合、SVD、人工证据类型或自动指标选择；标签只在全部评分后读取。
`root_route` 是检测候选，不是事实正确率；原始 R/A/H 和同窗口均值必须独立比较。
只做小模型/针对性软件验证。没有真实 8B/自然新 AUROC，就明确说明。
用户允许离线未来 token；不能擅自把方法限制为实时检测。
旧 transport 32答 AUROC/AP=0.718068/0.191248，raw=0.711559/0.185660，
离线均值=0.731726/0.186802；这些是用户提供的历史结果，不是新方法结果。
`transport --stage audit` 保留旧 `source_transport/` 的只读排名/AP/图状态审计。
审计不加载模型、不改分数；保存的 query 目标和 history key 标签应区分。
旧 v3–v6 标量滤波/融合和共享预算图评分代码已清理；历史实现见 Git `9d4ba17`。
`main.py dynamics` 是独立历史无标签状态实验，其概率不能自动解释成真假；
输入格式/历史证据见 `iclr/NATIVE_STATE_DYNAMICS.md` 和 `iclr/DYNAMICS_STATE_IDENTIFIABILITY.md`。
`main.py transport-state` 复用当前缓存做条件来源/历史状态审计，协议见
`iclr/CONDITIONAL_CHOICE_STATE.md`。`risk` 默认保持 raw_route，新增候选不自动替代。
`main.py transport-functions` 是独立的原生短语功能审计，协议见
`iclr/FUNCTIONAL_PHRASE_TRANSPORT.md`。v3 直接采集原始回答，不生成候选或用语义判断过滤原文。
按记录的原文区间总 log probability 计算梯度，不能冒称独立逐 token 真值梯度。
过长单元拆块不跳过；标题、标点、重复文本均测量，coverage 报告计划、实际与未选择 token。
Q/K、RMS、SwiGLU 不采用旧 value-path 归因规则；根敏感性不是守恒账本。
保留 residual/FFN-mediated 通道与末层 RMS 缩放，不能把任一负值直接叫反事实证据。
删除旧自动 bank 提案/评判链；v1/v2 的失败数据保留，不据原生敏感性宣称已解耦语义或提高 AUROC。
跨历史词的状态递推是额外检测假设，不能声称原生反传跨越了离散采样。
保留候选实际 ID、未解析尾部、来源支持/反对及当前头读取；不把来源根作用当作新读取事件。
保留用户缓存、原始结果及研究证据，不为美化改写历史；不全量重跑。
检测不以完成消融、所有因果机制或人工证据标注为前提。
不把最大 attention、正向作用、状态稳定或易预测自动命名为正确证据。
不另训真假分类器；若今后改为监督方法，必须明确改任务并分开汇报。
代码短函数、直接命名、职责分离；不堆框架、备用路径、重复检查。
复用 teaching 适配器；不使用 `set -e`，不后台跑实验，不改用户模型/结果文件。

## 2026-09-24：检测先验记录

后续检测设计先读 `iclr/DETECTION_PRIORS.md`，区分重复排序信号、单例观察、用户假设与失败候选。
熵首先作为生成阶段线索；FFN 可整合语义及调节置信度，不按梯度正负判事实支持。
检测不等待穷尽关系和机制；保留强基线，复杂状态模型必须展示同样本的实际收益。
文档中的条件状态建模是待验证方向，不能声称已经实现或获得新的 AUROC。

## 2026-09-24：输出可观测性候选

`main.py transport-observable` 实现原生累计导数的 Fisher 响应与多头结构条件核，
协议见 `iclr/OBSERVABLE_TRANSPORT.md`；此前“条件模型未实现”仅描述上一版状态。
不按原词梯度符号判事实，不把随机 probe 的正负解释为支持/抑制。
谱量来自输出加权的随机 pullback Gram，不是完整 Jacobian 的谱或非正规性。
论文 arXiv:2605.14258 的独立单 token block、残差坐标社区不能冒称本项目的完整上下文/token图。
来源隔离参考可混入错误；条件核分数不是幻觉概率，没有新的自然数据收益声明。
保留 raw_route 默认 risk、同窗口均值、逐 token AUROC/AP与自动打包；不全量重跑。

## 2026-09-24：用户授权监督表示读出

用户明确允许引入自然标签训练分类器，以检验现有表示是否包含可读出的检测信息。
`main.py transport-readout` 是独立监督诊断；以上无标签限制继续适用于既有无监督入口。
标签仅作为训练目标/评估真值，不生成特征、来源记忆或事实边界。
同来源全部回答整体留出；imputer/scaler/classifier只在训练来源拟合。
不随机拆token，不在本四答上调参选模型；保留失败结果、原分数和独立基线。
cohort条件核分数不入训练特征，避免其参考混入留出来源；不把监督成绩冒充无监督成绩。
完整表示读出复用已有逐头NPZ，无新大模型前向；仅有轻量包时不能声称检验了完整逐头信息。
分类成功只说明标签相关信息可读出，不证明适用证据、事实选择或因果继承。
公式、视图、评价与四答实测见 `iclr/SUPERVISED_RESPONSE_READOUT.md`。

## 2026-09-24：无标签双状态读出

`main.py transport-dual` 复用完整observable缓存，保留当前与逐头持续状态，协议见
`iclr/DUAL_STATE_READOUT.md`。不读监督预测，不用标签拟合/挑头/选窗口。
标量对照可用轻量包；其成绩不能冒充逐头校正的成绩。max保留当前数值，不保证排名或召回。
因果与离线分开保存；来源隔离的无标签参考不是全正常分布。原risk与缓存不改。

## 2026-09-24：来源／历史四条件候选

`main.py transport-contrast` 复用observable输入，测量同一原回答的来源有/无、远历史有/无四条件，
协议见 `iclr/EVIDENCE_CONTRAST.md`。主候选为两种历史下来源log概率作用的负对称平均。
不训练真假分类器、不生成或筛选原文；完整评分保存后才读取标签。保留原risk及同窗口基线。
来源删除改变长度/位置，不能称已隔离的事实因果效应；低来源作用不等于幻觉。
使用原生SDPA、分块logits与单条件KV；每个局部单元前裁回prompt，禁止共享被回答污染的缓存。
文本边界不是reanchor；单元/离线分数涉及未来文本。先验证实际检测收益，再考虑学生与大范围重构。

## 2026-09-24：关键历史消息与节点表征

`main.py transport-carriers` 复用 contrast/aggregation 的单元、原 token 与基线，协议见
`iclr/MESSAGE_CARRIER_REPRESENTATION.md`。单元平均原文logp的完整原生梯度筛选历史消息；
每头一个历史key、跨条件max绝对梯度选前K头，不读标签、不按符号判事实。
同组边在两个来源条件分别做单边、联合与同层头随机删除，后续网络原生重算。
保存固定物理层头轴的逐token向量、未测mask及历史边身份，不将单元梯度冒称逐token梯度。
SDPA稀疏消息减法保存重建误差，不能声称bf16与dense删边bit-exact。
所有评分后读标签，risk仍为raw_route；新增实测前不声称8B或自然数据收益。
