# 独立来源编辑、生成种子对照与单层穷举

2026-09-12，前两轮结果可见后追加，执行前冻结；探索性消歧，不伪装独立确认集。

全32层最终query X与端点交换已分别改变正确grill和错误onion数值选择。
但原来源交换同时改动了两个数值，且单层仅测过7/15/23/31。
本轮补齐这两个具体缺口，不开始拟合任何检测模型。

## S：两个来源数值与历史独立编辑

固定source14375的四个已有实际生成种子00012–00015，各执行A×B×H共8条件。
A只改prompt位置76的12→14；B只改位置341的14→12；H只改该回答先前
grill上界14→12（生成步分别99/100/98/109）。00012的A=B=0/1与首轮结果复现。
保留全部其他tokens。A/B单独改变时不保持全prompt数值多重集合，此变化显式记录；
A=B=1仍保持原多重集合。某些条件两处数值相同，不能当作有竞争的归属准确率。

读取窗口（均真实生成位置，预先检查token串）：

| trace | 正确grill上界 | 第二读取位置 |
|---|---:|---|
| 00012 |99|错误洋葱上界131，实际12，来源未给该动作合法时长|
| 00013 |100|正确洋葱继续煮后t126，实际“ over”，不应强加数值二选一|
| 00014 |98|受控负例simmer上界61，实际20，来自未修改的15–20|
| 00015 |109|受控负例另一grill上界142，实际10，来自未修改的5–10|

全词表logits、原生argmax/tie、JS、固定实际token logp都保存。
14−12候选差只解释两个目标数值窗口以及四条正确grill；其他控制使用全词表变化。
对每个窗口报告A/B/H平均主效应及所有原始条件，不用弱历史效应断言历史完全无关。
未来H编辑对其所在grill预测及之前读取位置必须逐logit严格不变。
四seed属于同一source，不当作四独立来源；新增正确窗口不能扩大错误窗口数。

## L：全部单层X

在00012的grill t99及onion t131，分别只在一个零基层0–31、最终query交换
S1H0来源V，共64条件。完整复用第二轮32层捐赠A/V文件与原始基线，重新加载同模型。
保存每次两个端点全词表logits、margin、argmax/tie与非空早期query最大误差。
四个此前单层查询条件的候选差必须复现；不挑最好层重新命名成预设主方法。
报告每层实际作用及“是否有单层足够翻转”，不把多层作用假定为各单层和。

合计32个完整输入世界 + 64次局部干预。仍不能由这组干预报告100%自动回看定位、
约束归属检测准确率、跨source泛化或图模型必要性。科学审稿REVIEW_UNAVAILABLE。

现有环境不变，在reanchor根目录使用新输出目录：

```bash
bash scripts/run_ownership_source_layer.sh --output outputs/ownership_source_layer_20260912
```
